name: Continuous Integration
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Set up JDK 21 LTS
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'    # ✅ Eclipse Temurin (OpenJDK)
          cache: 'maven'             # ✅ Cache do Maven para builds mais rápidos
          
      - name: Run tests with Java 21
        run: mvn clean test

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: success()
    steps:
      - name: Deploy application
        run: |
          echo "🚀 Deploying Java 21 application..."
          echo "✅ All tests passed! Ready for deployment."
          # Seus comandos de deploy aqui
